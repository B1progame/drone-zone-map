# Country mapping files

Copy `example.json` to `private-data/<country>/mapping.json`. Map exact official
category values to semantic categories and source field names to the common
schema. Classification uses legal/source text, never the website colour.
